#include<iostream>
using namespace std;

// 常函数：
// 1. 成员函数加const后，我们称该函数为常函数
// 2. 常函数内不可修改成员属性
// 3. 成员属性声明时加关键词mutable后，在常函数中可以修改

// 常对象
// 1. 声明对象前加const，称该对象为常对象
// 2. 常对象只能调用常函数

int main() {
	system("pause");
	return 0;
}